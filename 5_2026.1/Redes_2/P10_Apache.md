# 1. 
A resposta do sistema lista os pacotes disponíveis para o Apache. A importância disso é permitir ao administrador saber quais pacotes o sistema possui de uma aplicação e obter informações específicas sobre eles, como versão, tamanho e arquitetura.

# 2 
- Serão necessários 6 pacotes.
- A designação .i586 se refere à arquitetura (Architecture) para a qual o pacote foi compilado.
- O tamanho necessário é de 2 MB.
- A versão instalada é a 2.0.54-13mdk.
- Foram instalados os módulos libapr0, libapr-util0, apache-base, apache-conf, apache-modules e apache-mpm-prefork.
- Os pacotes foram instalados a partir do diretório /var/cache/urpmi/rpms.
- O arquivo de configuração do Apache foi alocado no diretório /etc/httpd/conf.

# 3
- O texto não exibe a saída exata no terminal após este comando, mas a função da ação é iniciar o serviço do Apache para que ele possa responder às requisições HTTP.
- O sistema exibe o documento HTML padrão (o arquivo index.html) que o Apache utiliza como "home-page" (contendo a mensagem "It works!").
- Se a página continuar exibindo a mensagem "It works!", isso significa que o browser (como o Mozilla Firefox) carregou a página antiga que estava armazenada em seu cache, em vez de carregar a versão recém-alterada.

# 4 
- Apache é um servidor Web que se caracteriza por ser uma aplicação que pode ser extremamente configurável, robusta e de alta performance. Sua principal funcionalidade é possuir múltiplas características configuráveis e ter código-fonte disponível para responder a requisições de páginas e serviços na rede.
